# 🚀 ckanext-advancedfilters 安装使用指南

## 📍 环境信息

- **CKAN访问地址**: https://localhost:8443/
- **部署模式**: 生产模式 (docker-compose.yml)
- **CKAN版本**: 2.11
- **插件状态**: ✅ 已安装，待启用

---

## 一、快速安装（3步完成）

### 第1步：启用插件

在容器中添加插件配置：

```bash
# 进入项目目录
cd /home/wenze/dockerhub/filter

# 配置插件（生产环境）
docker exec -u root filter-ckan-1 bash -c \
  "ckan config-tool /srv/app/ckan.ini \
  'ckan.plugins = image_view text_view datatables_view video_view audio_view webpage_view resource_proxy datastore datapusher envvars advancedfilters'"
```

### 第2步：重启服务

```bash
# 重启CKAN容器
docker-compose restart ckan

# 等待启动（约10秒）
sleep 10
```

### 第3步：验证安装

```bash
# 查看插件是否加载
docker logs filter-ckan-1 | grep -i "advancedfilters"

# 应该看到类似输出：
# Loading the following plugins: ... advancedfilters
```

✅ **完成！** 现在访问 https://localhost:8443/ 开始使用。

---

## 二、使用方法

### 在Web界面使用

#### 步骤1：打开数据集
1. 访问 **https://localhost:8443/**
2. 登录CKAN（如需要）
3. 找到你的 `spotify_analysis_dataset` 数据集
4. 点击进入资源详情页

#### 步骤2：使用高级筛选

⚠️ **重要提示：筛选面板位置**
- 筛选面板位于**数据表格的上方**
- 包含标题"高级筛选条件"和一个筛选图标 🔍
- 如果看不到，请：
  1. **刷新页面**（按 Ctrl+F5 强制刷新）
  2. 确认当前使用的是 **datatables_view** 视图（点击"Table"标签）
  3. 向上滚动页面查看表格上方区域

**筛选面板包含：**
1. 标题：**"高级筛选条件"** （带筛选图标）
2. 绿色按钮：**"+ 添加筛选条件"**
3. 绿色按钮：**"✓ 应用筛选"**
4. 灰色按钮：**"✗ 清除所有"**

#### 步骤3：添加筛选条件
1. 点击 **"+ 添加筛选条件"** 按钮
2. 会出现一行筛选表单，包含：
   - 字段选择下拉框
   - 操作符选择下拉框
   - 值输入框
   - 移除按钮

#### 步骤4：设置筛选条件

**示例1：筛选高人气歌曲**
- 字段: `popularity`
- 操作符: `大于`
- 值: `70`
- 点击 **"应用筛选"**
- ✅ 结果：只显示人气值大于70的歌曲

**示例2：筛选能量范围**
- 字段: `energy`
- 操作符: `范围`
- 最小值: `0.5`
- 最大值: `0.9`
- 点击 **"应用筛选"**
- ✅ 结果：只显示能量值在0.5-0.9之间的歌曲

**示例3：筛选特定艺术家**
- 字段: `artist`
- 操作符: `等于`
- 值: `Artist 7`
- 点击 **"应用筛选"**
- ✅ 结果：只显示该艺术家的歌曲

**示例4：多条件组合**
- 添加多个筛选条件
- 例如：`popularity > 70` **AND** `energy between 0.5-0.9`
- 点击 **"应用筛选"**
- ✅ 结果：同时满足所有条件的歌曲

---

## 三、支持的筛选操作符

### 📊 数值字段（popularity, energy, danceability等）

| 操作符 | 说明 | 示例 |
|--------|------|------|
| **等于** | = | `popularity = 70` |
| **不等于** | ≠ | `popularity ≠ 70` |
| **大于** | > | `popularity > 70` |
| **大于等于** | ≥ | `popularity ≥ 70` |
| **小于** | < | `popularity < 70` |
| **小于等于** | ≤ | `popularity ≤ 70` |
| **范围** | BETWEEN | `energy between 0.5 - 0.9` |
| **包含于** | IN | `popularity in [70, 80, 90]` |

### 📝 文本字段（artist, album, track_name等）

| 操作符 | 说明 | 示例 |
|--------|------|------|
| **等于** | 完全匹配 | `artist = Artist 7` |
| **不等于** | 不匹配 | `artist ≠ Artist 7` |
| **包含** | 模糊匹配 | `track_name 包含 Song` |
| **包含(不区分大小写)** | 忽略大小写 | `artist 包含 artist` |
| **包含于** | 多值匹配 | `album in [Album 3, Album 5]` |

### 📅 日期字段（release_date等）

| 操作符 | 说明 | 示例 |
|--------|------|------|
| **等于** | 特定日期 | `release_date = 2010-01-05` |
| **大于** | 晚于 | `release_date > 2010-01-05` |
| **小于** | 早于 | `release_date < 2010-01-05` |
| **范围** | 日期区间 | `2010-01-01 至 2010-01-10` |

---

## 四、通过API使用

### Python示例

```python
import requests
import json

# CKAN API端点
url = 'https://localhost:8443/api/3/action/advanced_datastore_search'

# 构建请求数据
data = {
    'resource_id': '你的资源ID',  # 从浏览器URL获取
    'advanced_filters': {
        'popularity': {
            'operator': 'gt',
            'value': 70
        },
        'energy': {
            'operator': 'between',
            'value': [0.5, 0.9]
        }
    },
    'limit': 20
}

# 发送请求（禁用SSL验证，因为使用自签名证书）
response = requests.post(url, json=data, verify=False)
result = response.json()

# 输出结果
if result['success']:
    print(f"找到 {len(result['result']['records'])} 条记录")
    for record in result['result']['records'][:5]:
        print(f"  ID: {record['_id']}, "
              f"歌名: {record['track_name']}, "
              f"人气: {record['popularity']}")
else:
    print(f"错误: {result.get('error', {})}")
```

### curl示例

```bash
curl -k -X POST "https://localhost:8443/api/3/action/advanced_datastore_search" \
  -H "Content-Type: application/json" \
  -d '{
    "resource_id": "你的资源ID",
    "advanced_filters": {
      "popularity": {
        "operator": "gt",
        "value": 70
      }
    },
    "limit": 10
  }'
```

**注意**: `-k` 参数用于跳过SSL证书验证（自签名证书）。

---

## 五、常见问题排查

### ❌ 问题1：看不到"高级筛选条件"面板

**可能原因**：
1. 插件未启用
2. 资源未推送到DataStore
3. 浏览器缓存

**解决方法**：
```bash
# 1. 确认插件已安装
docker exec filter-ckan-1 pip list | grep advancedfilters

# 2. 确认插件已配置
docker exec filter-ckan-1 grep "ckan.plugins" /srv/app/ckan.ini

# 3. 重启服务
docker-compose restart ckan

# 4. 清除浏览器缓存（Ctrl+Shift+Delete）
```

### ❌ 问题2：筛选后无结果

**可能原因**：
1. 筛选条件过于严格
2. 字段值类型不匹配
3. 数据中没有符合条件的记录

**解决方法**：
1. 放宽筛选条件测试
2. 检查字段类型（数值/文本/日期）
3. 先查看原始数据确认值的范围

### ❌ 问题3：容器启动失败

**检查命令**：
```bash
# 查看所有容器状态
docker ps -a

# 查看CKAN日志
docker logs filter-ckan-1 | tail -50

# 查看错误信息
docker logs filter-ckan-1 2>&1 | grep -i error
```

---

## 六、实用技巧

### 💡 技巧1：保存筛选条件

筛选条件会自动保存在URL中，你可以：
- 收藏页面
- 分享链接给他人
- 刷新页面后条件仍然保留

### 💡 技巧2：多条件组合

添加多个筛选条件实现复杂查询：
```
popularity > 70 
AND energy between 0.5-0.9 
AND danceability < 0.7
```

### 💡 技巧3：清除筛选

点击 **"清除所有"** 按钮可以一键移除所有筛选条件。

### 💡 技巧4：查看字段类型

在添加筛选条件时，系统会自动根据字段类型显示对应的操作符：
- 数值字段 → 显示大于、小于、范围等
- 文本字段 → 显示等于、包含等
- 日期字段 → 显示日期比较和范围

---

## 七、快速命令参考

```bash
# 项目目录
cd /home/wenze/dockerhub/filter

# 查看服务状态
docker-compose ps

# 重启CKAN
docker-compose restart ckan

# 查看日志
docker logs -f filter-ckan-1

# 进入容器
docker exec -it filter-ckan-1 bash

# 停止所有服务
docker-compose down

# 启动所有服务
docker-compose up -d

# 查看插件列表
docker exec filter-ckan-1 pip list | grep ckanext
```

---

## 八、获取资源ID

资源ID需要从浏览器URL获取：

1. 在CKAN中打开你的数据集
2. 点击资源名称
3. 查看浏览器地址栏URL
4. 格式类似：`https://localhost:8443/dataset/xxx/resource/382282c4-125f-49bf-aae7-c04f16ae8cc9`
5. 最后一段就是资源ID：`382282c4-125f-49bf-aae7-c04f16ae8cc9`

---

## 九、卸载插件（如需要）

```bash
# 1. 从配置中移除
docker exec -u root filter-ckan-1 bash -c \
  "ckan config-tool /srv/app/ckan.ini \
  'ckan.plugins = image_view text_view datatables_view video_view audio_view webpage_view resource_proxy datastore datapusher envvars'"

# 2. 重启服务
docker-compose restart ckan

# 3. 卸载插件（可选）
docker exec -u root filter-ckan-1 pip uninstall ckanext-advancedfilters -y
```

---

## 十、性能优化建议

### 为常用筛选字段添加索引

```bash
# 进入数据库容器
docker exec -it filter-db-1 psql -U ckandbuser -d datastore

# 为popularity字段添加索引
CREATE INDEX IF NOT EXISTS idx_resource_popularity 
ON "你的资源ID" (popularity);

# 为energy字段添加索引
CREATE INDEX IF NOT EXISTS idx_resource_energy 
ON "你的资源ID" (energy);

# 退出
\q
```

---

## 📚 相关文档

- **快速开始**: `QUICK_START.md`
- **详细部署指南**: `DEPLOYMENT_GUIDE.md`
- **完整安装说明**: `INSTALL_PLUGIN.md`
- **项目总结**: `PROJECT_SUMMARY.md`
- **插件README**: `src/ckanext-advancedfilters/README.md`

---

## 🔧 故障排除

### 问题1：看不到筛选面板

**症状**：打开资源页面后，数据表格上方没有"高级筛选条件"面板

**解决方案**：

```bash
# 步骤1：确认插件已加载
curl -k https://localhost:8443/api/action/status_show | grep advancedfilters

# 步骤2：强制刷新浏览器缓存
# 在浏览器中按 Ctrl+F5 或 Cmd+Shift+R

# 步骤3：检查模板文件
docker exec filter-ckan-1 ls -la /srv/app/src/ckanext-advancedfilters/ckanext/advancedfilters/templates/package/

# 步骤4：重启CKAN容器
docker restart filter-ckan-1

# 步骤5：等待启动完成（约15秒）
sleep 15

# 步骤6：再次访问资源页面并刷新
```

**检查要点**：
- ✅ 确认使用的是 **datatables_view** 视图（Table标签）
- ✅ 向上滚动查看表格**上方**区域
- ✅ 清除浏览器缓存和Cookie
- ✅ 检查浏览器控制台是否有JavaScript错误（按F12）

### 问题2：插件未加载

**症状**：API状态中没有 advancedfilters

**解决方案**：

```bash
# 检查.env配置
grep CKAN__PLUGINS .env

# 应该包含 advancedfilters

# 如果没有，更新配置
docker-compose down
# 编辑 .env 文件，在CKAN__PLUGINS中添加 advancedfilters
docker-compose up -d
```

### 问题3：筛选不生效

**症状**：点击"应用筛选"后没有反应

**解决方案**：

1. 检查浏览器控制台（F12）是否有错误
2. 确认字段名称正确
3. 确认输入值格式正确（数值字段输入数字，日期字段输入日期）
4. 查看CKAN日志：`docker logs filter-ckan-1 --tail 50`

---

## 🆘 技术支持

遇到问题？按以下步骤排查：

1. **查看日志**: `docker logs filter-ckan-1 | tail -100`
2. **检查配置**: `docker exec filter-ckan-1 cat /srv/app/ckan.ini | grep plugins`
3. **重启服务**: `docker-compose restart ckan`
4. **清除缓存**: 浏览器中按 Ctrl+Shift+Delete
5. **查看文档**: 阅读 `DEPLOYMENT_GUIDE.md`
6. **检查模板**: 确保模板文件正确加载

---

**插件版本**: 1.0.0  
**更新日期**: 2026-01-16  
**CKAN地址**: https://localhost:8443/  
**部署模式**: 生产环境 (docker-compose.yml)

✅ **祝使用愉快！**
